/**
 * `@couchcade/ui/pips`: the Interface Pip, the vector avatar for phones, menus and TV overlays
 * (docs/architecture/pips.md "Interface Pip"). Draws `@couchcade/theme`'s Pip part geometry, so
 * it never drifts from `@couchcade/stage`'s scoreboard heads (pips.md decision 10).
 */
export { default as CcPip } from "./CcPip.vue";
