{
  "name": "@couchcade/physics",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic 2D physics on Planck.js: a pure fixed-step world rebuilt from plain JSON body state. Tier 2 core, host only (never on phones).",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "planck": "catalog:"
  },
  "devDependencies": {
    "@couchcade/config": "workspace:*",
    "fast-check": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
