{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "timezone": "Europe/Amsterdam",
  "schedule": ["* 0-6 * * 1"],
  "minimumReleaseAge": "1 day",
  "internalChecksFilter": "strict",
  "postUpdateOptions": ["pnpmDedupe"],
  "packageRules": [
    {
      "description": "Versions live in the pnpm-workspace.yaml catalog, so updates land there and packages keep \"catalog:\". Minor, patch, pin and digest updates, dev dependencies and GitHub Actions included, come as one weekly PR.",
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "groupName": "non-major dependencies",
      "groupSlug": "non-major"
    },
    {
      "description": "Hold Vitest on 4.1.x: @cloudflare/vitest-plugin doesn't support Vitest 5 yet (docs/TECH_STACK.md).",
      "matchPackageNames": ["vitest", "@vitest/**"],
      "allowedVersions": "<4.2.0"
    }
  ]
}
