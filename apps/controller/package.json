{
  "name": "@couchcade/controller",
  "version": "0.0.0",
  "private": true,
  "description": "The phone app: join a room by QR code or code, the lobby, the waiting screens and the running game's controller. Served at /.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "vue-tsc",
    "test": "vitest run",
    "trace:record": "vite --config src/dev/trace-recorder/vite.config.ts"
  },
  "dependencies": {
    "@couchcade/game-sdk": "workspace:*",
    "@couchcade/motion": "workspace:*",
    "@couchcade/protocol": "workspace:*",
    "@couchcade/theme": "workspace:*",
    "@couchcade/ui": "workspace:*",
    "@couchcade/utils": "workspace:*",
    "partysocket": "catalog:",
    "vue": "catalog:"
  },
  "devDependencies": {
    "@couchcade/config": "workspace:*",
    "@vitejs/plugin-vue": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "vue-tsc": "catalog:"
  }
}
