[
  {
    "name": "Controller initial JS",
    "app": "controller",
    "kind": "platform",
    "gzip": true,
    "limit": "80 KB",
    "note": "README.md, Performance budgets > Phone controller > Initial JS (gzip)."
  },
  {
    "name": "Controller per-game chunk",
    "app": "controller",
    "kind": "per-game",
    "gzip": true,
    "limit": "25 KB",
    "note": "README.md, Performance budgets > Phone controller > Per-game controller chunk (gzip). Checked once per games/*/src/controller entry the controller build actually emits, discovered from the build output, not a maintained list."
  },
  {
    "name": "Controller fonts",
    "app": "controller",
    "kind": "fonts",
    "gzip": false,
    "limit": "40 KB",
    "note": "README.md, Performance budgets > Phone controller > Fonts (2 subsetted WOFF2 files). WOFF2 is already compressed, so this is the raw file size, not gzip."
  },
  {
    "name": "Controller Pip customiser chunk",
    "app": "controller",
    "kind": "lazy",
    "path": "apps/controller/src/pips/",
    "gzip": true,
    "limit": "4 KB",
    "note": "docs/architecture/pips.md, Budgets: \"Customiser screen as a lazy chunk <= 4 KB\". LobbyScreen.vue loads it through a dynamic import() (defineAsyncComponent), so it never reaches a phone that doesn't open \"Make your Pip\" and is excluded from Controller initial JS."
  },
  {
    "name": "Host platform JS",
    "app": "host",
    "kind": "platform",
    "gzip": true,
    "limit": "450 KB",
    "note": "README.md, Performance budgets > Host > Platform JS (gzip, incl. Phaser). Excludes each game's own host/ scene chunk, which is loaded per game, not on platform boot."
  },
  {
    "name": "Host Phaser chunk",
    "app": "host",
    "kind": "vendor",
    "vendor": "phaser",
    "gzip": true,
    "limit": "400 KB",
    "note": "Not a documented budget on its own (Phaser is folded into the 450 KB host platform JS total). Recorded here as an early-warning ceiling with headroom over the measured ~357.5 KB, so a Phaser version bump shows up as its own failure instead of only tripping the combined platform total. See CC-9.1 implementation notes."
  },
  {
    "name": "Host per-game chunk",
    "app": "host",
    "kind": "per-game",
    "gzip": true,
    "limit": "60 KB",
    "note": "Not a documented budget on its own. A game's src/index.ts (rules, physics included) now loads lazily (CC-3.25), so it lands in its own chunk instead of the 450 KB host platform JS total the same way its host/ scene chunk already did. Checked once per games/*/src/index.ts the host build actually emits a chunk for, discovered from the build output. Headroom over quick-draw's and target-range's measured 11.6 KB and 17.4 KB for a game with a heavier rules dependency, such as Strike Night's @couchcade/physics (Planck.js): CC-12.2 (not in this build) pushed the old, eager platform total up by about 32 KB before this story, so 60 KB leaves room for that plus growth."
  }
]
