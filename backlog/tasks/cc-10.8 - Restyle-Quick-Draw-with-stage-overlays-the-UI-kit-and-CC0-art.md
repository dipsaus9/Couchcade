---
id: CC-10.8
title: 'Restyle Quick Draw with stage overlays, the UI kit and CC0 art'
status: Done
assignee: []
created_date: '2026-09-16 12:25'
updated_date: '2026-09-16 22:46'
labels:
  - story
dependencies:
  - CC-10.6
  - CC-10.5
  - CC-4.4
  - CC-4.6
references:
  - games/quick-draw/src/host/
  - games/quick-draw/src/controller/
  - games/quick-draw/test/host/
  - games/quick-draw/package.json
parent_task_id: CC-10
type: feature
ordinal: 111000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The first game moves from placeholders to the Clubhouse look.

Type: deliverable
Branch: CC-10.8/quick-draw-restyle
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The host scene extends StageScene and uses stage callouts
- [x] #2 The controller uses the UI kit big action
- [x] #3 Sprites come from games/quick-draw/assets/
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Add @couchcade/stage to games/quick-draw deps. 2. host/sprites.ts: import the 12 PNGs from games/quick-draw/assets/sprites (Vite inlines them as data URIs, CSP img-src allows data:), texture keys prefixed quick-draw:, spritesheet frame sizes; preload skips keys already loaded. 3. host/world-pip.ts: pure 16x24 World Pip pixel grid from seat colour, skin/hair from the profile, expression and blink (placeholder until CC-6 World Pips), painted to canvas textures. 4. Rewrite host/scene.ts to extend StageScene: world from sprites (sky fill, mesa, speckled street tiles, rocks, cacti, tumbleweed, crow, sparkle, popgun-bang frames incl. BANG! flag, dust puff), Pips as images with a stage player shape marker; overlays on the stage layer: Scoreboard (points, round counter), DRAW!/fake words via addCallout (entrance seeked so the first frame is fully readable; shake started the next frame, none with reduced motion), FOUL!/time tags and the bottom instruction panel from stage drawSlab/textStyle, bottom-right kept free for the room code panel. Drop placeholder draw.ts/shapes.ts; keep present.ts/cues.ts pure (layout moves Pips up out of the bottom overlay row). 5. Update present/layout unit tests and the browser boot test (sprites loaded, stage overlays, round text, reduced motion first-frame DRAW!). 6. Verify AC2 (controller already on CcBigAction) with test evidence. 7. pnpm check, check:style, check:deps, test, build, e2e.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Verify: pnpm check && pnpm test

AC1: games/quick-draw/src/host/scene.ts extends StageScene (from @couchcade/stage, new workspace dep); DRAW! and the fake words use addCallout, the scoreboard uses addScoreboard, FOUL!/time tags, BANG! and the bottom instruction panel are built from stage drawSlab/textStyle on the stage overlay layer. The browser test asserts QuickDrawScene.prototype instanceof StageScene, a Scoreboard and a Callout on the overlay. DRAW! first-frame rule: the stage Callout pops from scale 0 (and fades from alpha 0 with reduced motion), which would hide DRAW! on its first frame; the scene kills that entrance and starts the Back.Out pop where it first reaches scale 1 (the overshoot plays after), shows it at once with reduced motion, and starts the 1 world px shake on the next frame. Fakes get the identical entrance so they can't be told apart. Test: first frame scale>=1, alpha 1, no shake; shake after; reduced motion: scale 1 and alpha 1 on every frame, camera never shakes.
AC2: already met by CC-10.3 and left unchanged: games/quick-draw/src/controller/Controller.vue imports CcBigAction from @couchcade/ui and renders <CcBigAction :state :label>; test/controller/Controller.test.ts asserts .cc-big-action with cc-big-action--waiting / --dont-tap classes (passes in pnpm test).
AC3: src/host/sprites.ts imports all 11 used PNGs from games/quick-draw/assets/sprites with ?inline (data URIs, CSP img-src allows data:), keys prefixed quick-draw:, preload skips loaded keys. ground-sand.png (Ink kerb edge) is not used: HOUSE_STYLE says no outlines on backgrounds. Browser test asserts every sprite key is loaded and tumbleweed, dust puff, popgun, crow and sparkle textures are visible in their phases; pixel reads find sky, sand, the street tile colour and Player 1's jersey.
World Pips: stage has no World Pips yet (CC-6.4), so src/host/world-pip.ts builds a 16x24 placeholder grid (ported from the design canvas worldPip: skin/hair from the profile, jersey in seat colour, Ink outline, neutral/happy/surprised/blink faces), painted once per look to a canvas texture; the seat shape is drawn under the feet with stage drawPlayerShape. Layout: Pips moved up (front feet y=200) so they clear the stage's bottom 15% row.
Sizes: host scene chunk 14.03 kB (5.62 gzip) -> 27.69 kB (12.20 gzip), of which ~4.4 kB is the inlined sprites and the rest the stage overlays; phaser chunk and the controller bundle unchanged (phones still get no stage/phaser).
Verify: pnpm check, check:style, check:deps, test (quick-draw 126 tests incl. 4 browser), build green; e2e quick-draw + smoke specs pass in chromium and webkit locally.

Review round 1 (story-reviewer): block on scope only. All 3 criteria met (tests and typecheck re-run green by the reviewer). Scope violations: games/quick-draw/package.json (the @couchcade/stage dependency the scene needs) and games/quick-draw/test/host/* (the host scene's unit and browser tests). Fix: References amended to add games/quick-draw/test/host/ and games/quick-draw/package.json, the same supporting paths the CC-10.4 review accepted for this scene.

Review round 2 (story-reviewer): pass. All 3 criteria met, no scope violations, no findings (reviewer re-ran vue-tsc and the 126 quick-draw tests). Follow-ups: (1) HostSceneData carries no room code, so the stage RoomCodePanel can't be shown during a game; the bottom-right safe-area corner is kept free for it (needs roomCode/joinUrl in game-sdk HostSceneData + apps/host). (2) The stage Scoreboard has no round-chip lift, so the spec's intro 'Round n chip lifts' is not shown. (3) CC-10.5's recolour turned Kenney's cactus greens mostly Sky; cacti read blue-green. (4) World Pips are placeholders until CC-6.4.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Quick Draw's TV scene now uses the Clubhouse look. QuickDrawScene extends StageScene: the stage Scoreboard shows chips and points with a round counter, DRAW! and the fake words use stage callouts (entrance skipped ahead so the word is fully readable on its first frame, identical for fakes, 1px shake the frame after, instant and still with reduced motion), and the FOUL!/time tags, BANG! and the bottom instruction panel are built from stage drawSlab/textStyle on the overlay layer. The world is built from games/quick-draw/assets sprites (mesas, speckled street tiles, rocks, cacti, tumbleweed, crow, popgun with BANG! flag, sparkle, dust puff), inlined as data URIs. World Pips are 16x24 placeholders from seat colour, profile skin/hair and expression, with the seat shape under their feet. present.ts and cues.ts stay pure (only the unrendered roundChipLift was dropped); placeholder draw/shapes/palette modules were removed. The browser boot test covers sprites, overlays, first-frame DRAW!, the shake, an 8-player match with every fake and reduced motion. The controller already used CcBigAction (AC2, unchanged). Scene chunk 14.03 -> 27.69 kB (gzip 5.62 -> 12.20). Review: pass in round 2 (round 1 blocked only on References, amended to include the host tests and package manifest).
<!-- SECTION:FINAL_SUMMARY:END -->
