---
id: CC-23.4
title: Build the Bandeja TV scene
status: Done
assignee: []
created_date: '2026-09-16 12:27'
updated_date: '2026-09-23 06:56'
labels:
  - story
dependencies:
  - CC-23.2
  - CC-1.15
  - CC-4.6
  - CC-3.8
references:
  - games/bandeja/src/host/
parent_task_id: CC-23
type: feature
ordinal: 200000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The TV renders Bandeja as a 480×270 pixel world.

Type: deliverable
Branch: CC-23.4/bandeja-host-scene
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 games/bandeja/src/host/ scene renders the state from the rules at 480×270 with integer scaling
- [x] #2 The scene extends StageScene and uses stage overlays for scores and callouts
- [x] #3 A headless boot test renders one full round without errors
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Verify: pnpm check && pnpm test

Resumed from a prior worker's uncommitted worktree (art.ts, cues.ts, layout.ts, overlays.ts, present.ts, sounds.ts, world.ts, test/host/): reviewed it in full, it was sound and spec-consistent (including finding 11's near-edge clear-space fix, already applied in layout.ts). Merged origin/main to pick up CC-23.8 (movement/CPU AI, merged after this branch's base) and updated present.ts + world.ts to render each slot's live BandejaState.positions instead of the old fixed home spots: PipActor now repositions its body and redraws its shape marker every frame (previously fixed-for-life since positions never changed pre-CC-23.8). Removed the now-dead slotPointsBySlot/slotPoints/SlotPoint helpers from layout.ts. pnpm check (repo-wide) and pnpm --filter ./games/bandeja test (60/60, incl. the browser boot test) both green. Known gap: the racket-swing sprite (racket-swing.png, 3 frames) is loaded but not yet drawn on a connect -- the spec's TV scene table calls for it, but attributing a hit to a specific slot/side-of-body isn't exposed by the current cue data, and rigging that up cleanly felt like its own slice of work; left as a follow-up rather than rushed in under this story.

Independent review (dipsaus-ai:story-reviewer) verdict: PASS -- all 3 acceptance criteria met, no scope violations, no findings; reviewer ran the actual browser test suite and confirmed it green. This session resumed after the implementer's worker died post-review: confirmed the branch was already rebased onto origin/main (30124fe sits directly on b13f5ec, CC-23.8's movement/CPU AI, so no further merge was needed), re-ran pnpm --filter ./games/bandeja run typecheck (clean) and run test (9 files / 60 tests passed, incl. the browser boot test), pnpm check (repo-wide, clean) and pnpm check:deps (no violations). pnpm test (full repo) hit one failure in games/target-range/test/host/scene.browser.test.ts ('starts without a room code panel...'); that file is untouched by this branch (no diff vs origin/main) and the same test passes cleanly in isolation, so it's pre-existing resource-contention flakiness under the full parallel monorepo run, not a regression from this story. Proceeding to close out and push.

Post-push CI (PR #176) failed the 'test' check: games/bandeja's own boot test (this story's AC #3) timed out at its 90_000ms budget, actual ~120s, when run alongside every other game's browser suite on the shared 2-core GitHub Actions runner. Passes reliably in isolation locally (~10s). Root cause: CC-23.8's per-tick movement/CPU AI now does real work every frame of the full-match tick loop, and CI runner contention inflates wall-clock far beyond a dedicated local run -- the same class of issue this repo already hit and fixed for quick-draw/target-range/strike-night (see commit chore/browser-test-timeout and their 120_000-240_000ms per-test overrides). Fixed by bumping this test's timeout from 90_000 to 180_000ms to match sibling games' headroom convention. Re-verified green locally (typecheck clean, 60/60 tests incl. this one); pushed as a follow-up commit and re-triggered CI.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Built the Bandeja TV scene: games/bandeja/src/host/ renders BandejaState at 480x270 with integer scaling, extends StageScene using its scoreboard/room-code/callout overlays plus the game's own instruction panel, and a headless Chromium boot test plays a full match through every phase without console errors. Implemented by a prior worker and independently reviewed (verdict: PASS, all 3 acceptance criteria met, no scope violations or findings, reviewer ran the browser suite). This session picked up after that worker died before delivery: confirmed the branch was already rebased onto origin/main (includes CC-23.8's movement/CPU AI), re-verified typecheck/test/check/check:deps green, then pushed and opened the PR.
<!-- SECTION:FINAL_SUMMARY:END -->
