---
id: CC-10.6
title: Register Quick Draw and add a bot-plays-a-match E2E test
status: In Progress
assignee: []
created_date: '2026-09-16 12:25'
updated_date: '2026-09-16 21:47'
labels:
  - story
dependencies:
  - CC-10.3
  - CC-10.4
  - CC-1.17
references:
  - games/quick-draw/src/index.ts
  - e2e/games/quick-draw.spec.ts
parent_task_id: CC-10
type: chore
ordinal: 109000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Quick Draw shows up in the game menu and is proven playable end to end.

Type: deliverable
Branch: CC-10.6/quick-draw-bot-match
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 games/quick-draw/src/index.ts exports the complete game definition and the registry picks it up
- [x] #2 e2e/games/quick-draw.spec.ts plays a full match with one host and two bot phones and asserts a winner
- [ ] #3 The E2E test passes in CI
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Contract: games/quick-draw/src/index.ts already default-exports the full defineGame definition (CC-10.2/10.4) and both registries glob it by folder; keep it, only document the registration. 2. e2e/games/quick-draw.spec.ts: host opens a room, Ana (VIP, joins first) and Ben join. No Start button exists on phones yet (CC-1.15/CC-1.16), so the spec routes Ana's relay socket with page.routeWebSocket and injects {t:ui:action,d:{action:start}} on it: the real relay and host path, no app change. 3. The phone never shows DRAW! (docs/games/quick-draw.md), so bots watch the TV: the spec wraps the host's phaserStage.start (same Vite dev module instance) to read the running game's getState, waits for phase draw, then Ana taps after ~250 ms and Ben after ~900 ms on the real big action. Fakes never enter phase draw, so bots never tap on a fake. 4. Assert phone result copy each round, then the final state (phase over, Ana 3 points, placements) and both phones back in the lobby. 5. Run pnpm e2e locally on Chromium and WebKit several times, then CI.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Verify: pnpm e2e

Implementation: games/quick-draw/src/index.ts was already the complete definition (CC-10.2/CC-10.4) and both registries glob it by folder; only a doc comment was added. e2e/games/quick-draw.spec.ts: host opens a room, Ana (VIP) and Ben join, Ana's relay socket is routed with page.routeWebSocket (reload after routing, since the route covers only later loads) and the spec sends {t:ui:action,d:{action:start}} on it. The phone never shows DRAW! (docs/games/quick-draw.md, so there is no act-now state on the phone): the bots watch the TV by wrapping phaserStage.start from /host/src/runtime/stage.ts (a dynamic import of the same Vite dev URL returns the running module instance) to read the game's getState, wait for phase draw with rAF polling, then Ana taps 250 ms and Ben 900 ms after DRAW! on the real big action. Fakes stay in phase standoff, so bots never tap one. Asserts per round Ana 'You won the round!' and n points, Ben 'Ana was faster' or 'Too slow', then state over with Ana 3 / Ben 0, the TV lobby and both phones back on the lobby heading. Local: CI=1 pnpm e2e 3 runs in a row, 6/6 passed each (about 33 s, no retries); the match test takes about 28 s per browser. Follow-up for CC-3.2/CC-4.8: when the phone gets a real Start/game-menu button, replace the routed-socket start with a tap. Coupling: the spec reads apps/host's phaserStage module path; if CC-4.6/CC-3.x moves it, update watchTv.

Review gate round 1 (dipsaus-ai:story-reviewer): pass. AC1-3 met (AC3 pending the PR's CI run), no scope violations, no findings. pnpm-lock.yaml (unchanged) and the task file were excluded from the reviewed diff on purpose.
<!-- SECTION:NOTES:END -->
