---
id: CC-23.8
title: Add automatic player movement and CPU partner AI to Bandeja
status: Done
assignee: []
created_date: '2026-09-16 12:27'
updated_date: '2026-09-21 14:48'
labels:
  - story
dependencies:
  - CC-23.2
references:
  - games/bandeja/src/shared/ai/
parent_task_id: CC-23
type: feature
ordinal: 204000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Players only swing; movement and empty slots are handled by AI.

Type: deliverable
Branch: CC-23.8/bandeja-cpu-movement
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 games/bandeja/src/shared/ai/ moves each player toward the ball's landing spot
- [x] #2 A CPU fills empty doubles slots and returns balls at a set difficulty
- [x] #3 Unit tests cover landing prediction
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. games/bandeja/src/shared/ai/landing.ts: predictLanding(ball, dtMs) - simulates the ball forward
   with physics.ts's own stepBallPlan/stepHeight (same 60 Hz, same predictSteps horizon as
   physics.ts's predict()) to the first floor bounce, returning {x, y, tMs} or null. Distinct
   concept from predict()'s reach-window arrival (which stays untouched, per the spec's own note
   that predict() "is also what CC-23.8 will use... which is why it belongs in shared/ and not
   inside CC-23.8's ai/ folder"). Unit tests: games/bandeja/test/ai/landing.test.ts (AC3).
2. games/bandeja/src/shared/ai/movement.ts: stepToward/clampToReach, pure vector helpers. A moving
   player is always clamped within its slot's fixed reach circle (rule 3's reach math in
   applySwing/predict is NOT changed - movement is layered on top, never promises a reach the hit
   test doesn't back up).
3. games/bandeja/src/shared/ai/positions.ts: nextPosition(spec, current, ball, dtMs) - walks a slot
   toward predictLanding(ball)'s spot (clamped to reach) while a ball's live, drifts back to spec.home
   otherwise. Wires into state.ts (BandejaState.positions, set by init()) and rules.ts (onTick
   advances positions once per tick before the phase switch; every existing downstream spread
   preserves it, so no other rules.ts function needs an explicit change). AC1.
4. games/bandeja/src/shared/ai/cpu.ts: cpuShot(state, spec) - replaces rule 10's stand-in
   (performAutoHit in rules.ts, currently always "ok" grade / aim 0). One fixed CpuDifficulty
   constant (a "set difficulty", not selectable) turns into a real grade via the same
   clean/ok/mishit timing bands a human swing uses (a fixed reaction-offset, deterministic - no new
   RNG per Fairness rule 4/rule 14), and real aim: toward whichever half of the receiving side's
   court its slots cover least (deterministic from state, not random). Removes constants.ts's now-
   unused autoReturnAngle (autoReturnSpeed stays, reused as the CPU's base pace). Updates
   rules.test.ts's "straight down the middle" test to the new, real aim (the spec's own rule 10
   text says CC-23.8 replaces exactly this: "no movement, no difficulty, and no aim"). AC2.
5. games/bandeja/src/shared/ai/index.ts barrel; games/bandeja/src/shared/index.ts re-exports it
   (the one shared/-level wiring touch outside ai/, alongside state.ts/rules.ts/constants.ts above -
   all within games/bandeja/src/shared/, per the story's own note that touching shared/ outside ai/
   to wire it in is expected). Does not touch controller/ or host/ (CC-23.3/CC-23.4, concurrent).
6. Verify: pnpm check && pnpm test (repo baseline), scoped to games/bandeja during the loop.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Verify: pnpm check && pnpm test

Implemented games/bandeja/src/shared/ai/: landing.ts (predictLanding, a new floor-bounce prediction reusing physics.ts's stepBallPlan/stepHeight - distinct from physics.ts's own predict(), which stays untouched per the spec's own note that CC-23.8 reuses it rather than changing it), movement.ts (stepToward/clampToReach vector helpers), positions.ts (nextPosition - walks a slot toward the ball's predicted landing spot, clamped inside its fixed reach circle, drifts home otherwise), cpu.ts (cpuShot - the real CPU: a fixed 'set difficulty' graded through the same clean/ok/mishit bands a human swing uses, and an aim that reads which half of the receiving side is covered least, deterministic - no new RNG, per Fairness rule 4/rule 14). Wired into shared/state.ts (BandejaState.positions, set by init(), NOT part of snapshot/restore - consistent with the existing 'no positions' restore note) and shared/rules.ts (onTick advances positions every tick; performAutoHit now calls cpuShot instead of always launching 'ok' grade at aim 0). Removed constants.ts's now-unused autoReturnAngle. Updated rules.test.ts's old 'straight down the middle' assertion (still passes unmodified in that specific 3-player symmetric-coverage scenario; the new asymmetric-aim behaviour is covered by test/ai/cpu.test.ts). Added games/bandeja/test/ai/{landing,movement,positions,cpu}.test.ts (mirrors the test/host/, test/controller/ subfolder convention other games already use). Did not touch controller/ or host/ (CC-23.3/CC-23.4, concurrent). Verify green: pnpm check && pnpm test (whole repo), plus pnpm run check:deps (no architecture violations).

Independent review (dipsaus-ai:story-reviewer, model sonnet): verdict PASS. All 3 acceptance criteria met, no scope violations, no findings. Reviewer independently confirmed: controller/ and host/ untouched; positions correctly excluded from snapshot/restore; 55/55 tests pass and typecheck clean; the frozen CC-23.2 reach-circle hit test (applySwing/predict) is untouched - movement is a cosmetic/AI-target layer on top, never granting a reach the hit test doesn't already allow.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added games/bandeja/src/shared/ai/ with real automatic player movement and a real CPU partner, replacing CC-23.2's fixed-home/auto-return stand-in. landing.ts predicts where the ball will next hit the floor (reusing physics.ts's own step functions, distinct from physics.ts's predict()); movement.ts/positions.ts walk each slot toward that landing spot each tick, always clamped inside its existing fixed reach circle so the frozen CC-23.2 hit test (applySwing/predict, still keyed to home spots) is never contradicted; cpu.ts replaces performAutoHit's old fixed 'ok grade, aim 0' stand-in with a real, deterministic CPU (a fixed set difficulty graded through the same clean/ok/mishit bands a human swing uses, aiming at whichever half of the receiving side is covered least - no new RNG, per Fairness rule 4). BandejaState gained a positions field (set by init, intentionally left out of snapshot/restore like the ball). Added games/bandeja/test/ai/{landing,movement,positions,cpu}.test.ts and updated one rules.test.ts assertion whose old comment described the now-replaced stand-in behaviour. Independent review (dipsaus-ai:story-reviewer): PASS, all 3 acceptance criteria met, no scope violations. Full repo pnpm check && pnpm test green, plus pnpm run check:deps clean. Did not touch controller/ or host/ (CC-23.3/CC-23.4, delivered concurrently).
<!-- SECTION:FINAL_SUMMARY:END -->
