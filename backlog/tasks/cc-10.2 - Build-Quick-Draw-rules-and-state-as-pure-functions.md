---
id: CC-10.2
title: Build Quick Draw rules and state as pure functions
status: Done
assignee: []
created_date: '2026-09-16 12:25'
updated_date: '2026-09-16 17:09'
labels:
  - story
dependencies:
  - CC-10.1
  - CC-1.13
  - CC-1.7
  - CC-1.14
references:
  - games/quick-draw/package.json
  - games/quick-draw/src/index.ts
  - games/quick-draw/src/shared/
  - games/quick-draw/test/
parent_task_id: CC-10
type: feature
ordinal: 105000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Deterministic Quick Draw game logic with its package scaffold.

Type: deliverable
Branch: CC-10.2/quick-draw-rules
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 games/quick-draw/src/shared/ implements init, onPlayerInput and (if real-time) onTick as pure functions following docs/games/quick-draw.md
- [x] #2 Randomness uses the seeded RNG and time arrives as dtMs; no Math.random or Date.now
- [x] #3 Unit tests cover scoring and win conditions
- [x] #4 testGameContract passes for the game definition
- [x] #5 One recorded input replay reproduces an exact final state
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Scaffold games/quick-draw (package.json @couchcade/game-quick-draw, tsconfig, vitest.config.ts) by hand; src/index.ts = defineGame over src/shared with a rejecting hostScene loader until CC-10.4 (no host/ folder yet).
2. src/shared/: constants, input schema (zod/mini), state + init, standoff roll (memoryless 1-in-90 per tick wait 2-8 s, fakes 0/1/2 at 30/50/20% from round 2, word/crow/glint 50/25/25, seed-derived word cycles, 1,000 ms gaps), rules (onPlayerInput foul/fooled/valid/slow, onTick phases intro/standoff/draw/result/over, onPlayerLeft), view (qd-watch/qd-standoff/qd-result, sends only on change), outcome (points then best reaction, shared places), snapshot/restore (changes only when a round resolves, under 300 bytes).
3. test/: rules unit tests (fake-outs, false starts, ties, leaving, win at 3, 9-round cap), view/outcome/snapshot tests, testGameContract, one recorded replay JSON with expected final state, fast-check determinism property.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Verify: pnpm check && pnpm test
Create the package by hand (the create-game template CC-3.11 may not exist yet).

hostScene: defineGame requires it, so src/index.ts uses a rejecting loader (as the SDK fixtures do) until CC-10.4 adds src/host/. No controller entry yet (CC-10.3).
Implied scaffolding beyond References: games/quick-draw/tsconfig.json and vitest.config.ts; pnpm-lock.yaml.
Snapshot changes only when a round resolves (session-flow.md) and is under 300 bytes for 8 players; the fake word order derives from the seed so a restore keeps it.
Time: state.nowMs accumulates dtMs; phase due times get half a tick of slack for float sums. Taps are judged on ctx.atMs against drawAtMs; displayLagMs is ignored per the spec.

Review (story-reviewer, round 1): pass. All 5 criteria met, no scope violations, no findings. Reviewer ran the package tests (68/68), pnpm check and pnpm check:deps.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added games/quick-draw (@couchcade/game-quick-draw) with Quick Draw's rules as pure functions over plain JSON state, following docs/games/quick-draw.md: a 1,500 ms intro, a memoryless standoff (2 s plus a 1-in-90 chance per tick, capped at 8 s) rolled from the seeded RNG in state, fake-outs from round 2 (0/1/2 at 30/50/20%, word/crow/glint at 50/25/25, seeded word cycles, 1,000 ms gaps, dropped when they don't fit), taps judged on ctx.atMs against DRAW! (foul under 100 ms, fooled within 1,000 ms after a fake, valid to 1,500 ms, else slow), exact-millisecond ties, first to 3 or 9 rounds, onPlayerLeft keeping points and ending under 2 players, phone views that change only twice per round, placements tie-broken by best reaction, and snapshot/restore that changes only when a round resolves. src/index.ts defines the game with a rejecting hostScene until CC-10.4. 68 tests: rules, fake-outs, views, snapshots, testGameContract, a recorded 3-player replay and fast-check determinism properties.
<!-- SECTION:FINAL_SUMMARY:END -->
